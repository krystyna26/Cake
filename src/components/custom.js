import React, { Component } from "react";
import Button from "../shared/NavButtons";

class Custom extends Component {
  render() {
    return (
      <div>
        <Button>I can see Custom component</Button>
      </div>
    );
  }
}

export default Custom;
